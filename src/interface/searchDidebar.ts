

export type Gender = {
    nombre: string;
    slug: string;
    count: number;
}


export type Categories = {
    nombre: string;
    slug: string;
    count: number;
}

export type Subcategorias = {
    nombre: string;
    slug: string;
    count: number;
}